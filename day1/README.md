# Day 1 — Bags of Words

The foundations: getting text into Python, cleaning it, turning it into numbers, and running
the classic bag-of-words methods.

## Slides
1. **Introduction** — welcome, what computational text analysis is, quant vs. qual
2. **Python & Colab** — the tools we'll use
3. **Classic Text Analysis** — the document-term matrix, TF-IDF, prerequisites
4. **Bag-of-Words Approaches** — dictionaries, supervised ML, topic modeling
5. **The Limits of Bag-of-Words** — what these methods throw away

## Notebooks
1. **Intro** — strings, tokenization, preprocessing, word frequencies, plots & word clouds
2. **Classic CTA** — building a document-term matrix on real data (inaugural addresses)

Each notebook comes with an `_exercises` version and a matching `_solutions` version.

Runs on a normal CPU runtime.
